apiVersion: 1
datasources:
  - name: Infinity
    uid: infinity
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: true
    jsonData:
      allowed_hosts:
        - "http://localhost:3002"
        - "https://cdn.jsdelivr.net"
        - "https://raw.githubusercontent.com"
